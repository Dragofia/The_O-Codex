Put your entry images in this folder and reference them from `world-data.json`.

Examples:

- `images/cooper-vale.png`
- `images/characters/crownhaven-gate.jpg`

If an entry has no image, the site will show a built-in fallback style instead.
